import { Meta, StoryObj } from "@storybook/react";

import { Opening } from ".";

const meta: Meta<typeof Opening> = {
  component: Opening,
};

export default meta;

type Story = StoryObj<typeof Opening>;

export const Default: Story = {};
